/**
 * Configure which sections of the index page are visible.
 * Also affects visible menu items in Navigation component.
 */
export const sections = {
    landing: true,
    about: true,
    blog: false,
    contact: true
};